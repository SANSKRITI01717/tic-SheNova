<div class="sidebar">

  <!-- Sidebar brand strip -->
  <div class="sidebar-brand-strip">
    <div class="sidebar-bloom">🌸</div>
    <div class="sidebar-brand-text">
      <div style="font-family:var(--font-display);font-size:0.95rem;font-weight:700;color:var(--lav-700);">Herlytics</div>
      <div style="font-size:0.7rem;color:var(--text-muted);">your cycle companion</div>
    </div>
  </div>

  <div class="sidebar-section">
    <div class="sidebar-label">Overview</div>
    <a href="/dashboard" class="sidebar-link <%= typeof activePage !== 'undefined' && activePage === 'dashboard' ? 'active' : '' %>">
      <span class="icon">🏠</span> Dashboard
    </a>
    <a href="/insights/results" class="sidebar-link <%= typeof activePage !== 'undefined' && activePage === 'results' ? 'active' : '' %>">
      <span class="icon">📊</span> My Results
    </a>
    <a href="/dashboard/report" class="sidebar-link <%= typeof activePage !== 'undefined' && activePage === 'report' ? 'active' : '' %>">
      <span class="icon">📋</span> Health Report
    </a>
  </div>

  <div class="sidebar-section">
    <div class="sidebar-label">Track</div>
    <a href="/insights/cycle" class="sidebar-link <%= typeof activePage !== 'undefined' && activePage === 'cycle' ? 'active' : '' %>">
      <span class="icon">🌸</span> Cycle Log
    </a>
    <a href="/insights/nutrition" class="sidebar-link <%= typeof activePage !== 'undefined' && activePage === 'nutrition' ? 'active' : '' %>">
      <span class="icon">🥗</span> Nutrition
    </a>
  </div>

  <div class="sidebar-section">
    <div class="sidebar-label">Share</div>
    <a href="/doctor" class="sidebar-link <%= typeof activePage !== 'undefined' && activePage === 'doctor' ? 'active' : '' %>">
      <span class="icon">👩‍⚕️</span> Doctor Share
    </a>
  </div>

  <div class="sidebar-section">
    <div class="sidebar-label">Account</div>
    <a href="/pin" class="sidebar-link">
      <span class="icon">🔐</span> Set PIN
    </a>
    <form action="/logout" method="POST" style="margin:0">
      <button class="sidebar-link w-full" style="background:none;border:none;cursor:pointer;text-align:left;" type="submit">
        <span class="icon">🚪</span> Logout
      </button>
    </form>
  </div>

  <!-- Soft motivational footer -->
  <div class="sidebar-footer">
    <div class="sidebar-footer-card">
      <div style="font-size:1.4rem;margin-bottom:0.3rem;">💜</div>
      <div style="font-size:0.75rem;font-weight:600;color:var(--lav-700);margin-bottom:0.2rem;">You're doing great</div>
      <div style="font-size:0.68rem;color:var(--text-muted);line-height:1.4;">Tracking your cycle is an act of self-love.</div>
    </div>
  </div>

</div>

<style>
.sidebar {
  width: 240px; flex-shrink: 0;
  background: linear-gradient(180deg, #fff 0%, var(--lav-50) 100%);
  border-right: 1.5px solid var(--lav-200);
  padding: 1.25rem 0.9rem 1rem;
  position: sticky; top: 64px; height: calc(100vh - 64px);
  overflow-y: auto;
  display: flex; flex-direction: column;
}

.sidebar-brand-strip {
  display: flex; align-items: center; gap: 0.6rem;
  padding: 0.6rem 0.75rem;
  background: linear-gradient(135deg, var(--lav-100), #fce7f3);
  border-radius: 12px; margin-bottom: 1.25rem;
  border: 1px solid var(--lav-200);
}
.sidebar-bloom {
  font-size: 1.4rem;
  animation: gentleSpin 6s linear infinite;
  display: inline-block;
}
@keyframes gentleSpin {
  0%   { transform: rotate(0deg);   }
  25%  { transform: rotate(8deg);   }
  50%  { transform: rotate(0deg);   }
  75%  { transform: rotate(-8deg);  }
  100% { transform: rotate(0deg);   }
}

.sidebar-section { margin-bottom: 1.25rem; }
.sidebar-label {
  font-size: 0.67rem; font-weight: 700; letter-spacing: 0.1em;
  text-transform: uppercase; color: var(--lav-400);
  padding: 0 0.75rem; margin-bottom: 0.4rem;
}

.sidebar-link {
  display: flex; align-items: center; gap: 0.65rem;
  text-decoration: none; color: var(--text-muted);
  padding: 0.58rem 0.75rem; border-radius: 10px;
  font-size: 0.88rem; font-weight: 500; transition: all 0.2s;
  margin-bottom: 0.1rem; width: 100%;
}
.sidebar-link:hover {
  background: linear-gradient(135deg, var(--lav-50), #fce7f3);
  color: var(--lav-700);
  transform: translateX(3px);
}
.sidebar-link.active {
  background: linear-gradient(135deg, var(--lav-100), #fce7f3);
  color: var(--lav-700); font-weight: 600;
  border-left: 3px solid var(--lav-500);
  padding-left: calc(0.75rem - 3px);
  box-shadow: 0 2px 10px rgba(139,92,246,0.1);
}
.sidebar-link .icon { font-size: 1rem; width: 20px; text-align: center; }

.sidebar-footer { margin-top: auto; padding-top: 1rem; }
.sidebar-footer-card {
  background: linear-gradient(135deg, var(--lav-100), #fce7f3);
  border: 1px solid var(--lav-200);
  border-radius: 12px; padding: 0.85rem 0.9rem;
  text-align: center;
}
</style>
